---
topic: Android Chase book
updated: 2026-09-06T12:20
---

- (event) sensei-advisor verdict=approve on prd r2. Spec starts.
- (decision) Yaml /3 is the only parse/merge home. Windows is a peer. Drop TS function names as source of law.
- (decision) Android-only yaml clause: after merge confirm as-of becomes max(prior, max applied trade_date). Windows snapshot as-of stays.
- (decision) Quantity is integer ten-thousandths. Avg cost stays cents. No Double lot qty on disk.
- (decision) Refuse codes: trades_without_book, missing_book_as_of, empty_keep, ledger_apply_unsupported, unreadable, as_of_unparseable.
- (capability) CAP-1 name kind before write
- (capability) CAP-2 snapshot confirm replace unique ticker
- (capability) CAP-3 window merge skip trade_date<=as-of then advance as-of
- (capability) CAP-4 durable unique ticker SQLite
- (capability) CAP-5 Held exact match flag
- (capability) CAP-6 pin is paint pre-pin rank
- (constraint) Compose does not decide Held. Pin helper in core. OpportunityEngine unchanged.
- (constraint) Live make android-run-qa only. Never pm clear. Never sp500.
- (event) SPEC draft plus examples.md. Next Sensei+Advisor on spec.
- (event) sensei-advisor verdict=revise on spec r1. Advisor P0 A-16 mixed quantity units.
- (decision) Yaml share quantity is the golden. SQLite ten-thousandths at persist boundary only.
- (decision) examples.md does not copy yaml parse expects. MERGE-REPLAY is Confirm-scoped.
- (event) sensei-advisor verdict=approve on spec r2. Build starts.
- (decision) CAP-7 Positions tab paints every lot. Off-feed PHYL is a row. Qty is shares from ten-thousandths. Import book lives here too.
- (capability) CAP-7 Positions tab shows the full book
- (capability) CAP-8 Positions closeness Today Tomorrow This week Later from report date
- (capability) CAP-9 Positions reuses Opps flags when a scored row exists
- (constraint) Closeness is calendar identity. No frozen percent. No invented date. No invented Act on off-feed lots.
- (decision) Retract non-goal New Portfolio tab. Keep no auto-add to feed. Keep no score change.
- (decision) Held mark omitted on Positions. Sort closeness then ticker. Blank last. UTC LocalDate. Sunday-Monday is Tomorrow.
- (constraint) Import book is one writer. Earnings, System, and Positions call it. Restore log never plans a lot write.
- (constraint) Core projects Positions rows and closeness. Compose paints. Off-feed lots are a row shape without score fields.
- (decision) P&L overlay out. Off-feed tap does not add to Yahoo. Positions sits next to Earnings.
- (decision) Closeness clock is EXCHANGE_ZONE America/New_York. Supersedes UTC today. Cases CLOSE-* use that zone.
- (event) Self-validate pass 1 coherence: CAP-7/8/9 have intent and success. CAP ids preserved. Portfolio tab non-goal retracted.
- (event) Self-validate pass 2 preservation: FR-7/8/9, four tags, Opps strip list, off-feed blank, Import third caller, SM-3, SM-C3 landed in SPEC.md and examples.md.
- (event) sensei-advisor verdict=revise on spec r3. Advisor wins A-19 A-20. Sensei S3-02 demoted to P1 A-22.
- (decision) A-19 A-20 absorbed. P1 Cases: POS-PHYL-BOARDS POS-SORT-ORDINAL POS-IMPORT-NONEMPTY CLOSE-FRI-MON CLOSE-SAT-MON CLOSE-YAHOO-PAST CLOSE-TZ. SM-3 live split.
- (event) sensei-advisor verdict=approve on spec r4 Positions. Build may start. Carry S-2 S4-01 S4-03 S4-04.
- (event) Positions tab built. Core closeness enum. Import third caller. Off-feed tap no-op.
- (event) Positions remaining Cases covered: CLOSE-* one test each, CLOSE-TZ Madrid default + paint Today, POS-NO-HYDRATE, POS-PHYL-BOARDS, POS-FLAGS-AMZN paint, POS-TAP-PHYL paint no-op. :core:test green. Targeted app Positions tests green. Live QA still open.
- (event) Positions tab shipped. Live qa: PHYL 1273 no Act no Detail. AMZN tokens match Opps. scoredLots overlays Opps onto universe.
- (event) 2026-09-06 review: changes required. See review-2026-09-06.md. Source defects include profile market overlap, stale calendar suppression, settled-date precedence, and small-position loss. Selected app suites and core suite pass. Offline SP500 budget passes. Installed QA inspection covers display and scroll only.
- (event) 2026-09-06 fixes split into Luna/max workers with exclusive file ownership. See luna-fix-ownership.md. Two workers started. Agent service rejected a third due to its thread limit. Profile work follows in a serial wave. Parent owns Gradle and shared docs. Preserve partial first-worker edits.
- (decision) 2026-09-06 doc reconciliation: PRD now matches existing shared-calendar spec behavior. Missing or expired lot dates use the calendar owner without feed growth. Freshness stays with that owner. Old dates do not hide eligible future evidence. Current fixes remain open until test and review evidence exists.
- (event) 2026-09-06 parent test evidence: PositionsPresentationTest found the same-day settled regression. EarningsEventRecorderTest reproduced expired positive and negative cache defects. Windows import tests found a zero-quantity row and one incorrect open-date expectation. Android core import passed; app import tests found blocked-read cancellation and stale-read generation defects. Workers received all results.
- (decision) Juan directs workers to own most work. Workers now own edit, test, refactor, and reports. They serialize Gradle with one named mutex. Calendar checks pass; that worker completes its report then takes profile-load ownership. Parent handles shared documents and final review.
- (event) Final source review required three R7 fixes: cleanup after spool creation failure, strict EOF validation, and isolated concurrent stage sessions. Worker supplied red and green evidence for each. Final source review accepts those fixes. Sink commits remain per chunk. R8 sustained-rate policy and R9 typed load status remain pending requirements. Book worker owns the final post-R7 APK build.
- (event) Luna/max worker handoff: import/calendar and profile R1/R7 fixes complete at source level. Full core passed 1609 tests with 16 skips. Full app passed before R7 review refinements; affected R7 suites passed afterward. Post-R7 assembleDebug passed. APK version 2026.09.06.0832.feat-earnings-gate-identities.db23aa88-dirty. Device QA, R8 sustained-rate policy, R9 typed load status, and broader compact-row UX remain open. No live Russell speed claim.
- (decision) Juan clarifies role ownership: parent owns design, architecture, acceptance criteria, scope, and release verdict. Luna/max workers implement defined work and report test evidence. Release worker finishes authorized qa device checks and restores the original book. R8 and R9 design stays with the parent. No worker can choose a scope deferral or approve release.
- (decision) Parent release review finds a refresh cancellation gap: loadStarted precedes cancellable stateMutex acquisition outside try/finally. Luna receives a bounded bugfix design: enter try immediately after loadStarted, track pass registration, and balance both counters in NonCancellable cleanup. Require deterministic cancellation before and after registration plus later-refresh recovery. This is a source defect; linkage to the observed suite timeout remains unproven. Release remains open pending regression and final-artifact checks.
- (event) Parent reviews the refresh cancellation patch and accepts its bookkeeping structure. Deterministic pre-registration red leaves loadInFlight true; dedicated green passes three cases. Post-registration case excludes detail enrichment and proves refresh cancellation only. Original full-detail replacement case remains. Full suite, final signed-artifact smoke, and an observed enrichment signal transition still need evidence before release closure.
- (decision) Final signed APK and installed QA debug APK have different certificates. Parent rejects uninstall or data wipe. Worker will attempt signed-artifact QA on a separate temporary AVD using an existing system image, distinct data and port, and explicit qa launch. The current emulator retains its restored 84 lots and as-of 2026-09-06. If local resources prevent isolation, report that constraint without changing signing configuration.
- (decision) Juan clarifies personal delivery: debug APK sideloads are the default. Custom signed releases remain supported but are optional and require an explicit request. AGENTS.md now records this rule. This supersedes the signed-artifact gate and temporary AVD plan in entry 48. Worker stops that work and reports final debug APK evidence. Existing test, qa profile, and data preservation rules remain.
- (event by luna) 2026-09-06 Luna fixed refresh cancellation bookkeeping: deterministic pre-registration red showed loadInFlight stuck; focused 3-case suite green; full app suite passed 1216 tests with 0 failures, 0 errors, and 3 skips.
- (event by luna) 2026-09-06 Luna installed final QA debug APK on emulator-5554 with data preserved; QA chip and restored Positions 84 with as-of 2026-09-06 remain visible. Debug artifact SHA-256 CABFD604DD29468E008B3C5738F9AF9E981B38BD095B373B6323AD4E782ABBA0. A temporary release-smoke AVD was created and booted, then stopped after Juan made custom signing optional; no release APK was installed there.
- (decision) Parent accepts the implemented bugfix patch for personal debug sideload. Full app suite passes 1216 tests with 3 skips; core passes 1609 with 16 skips. QA report records 16 device passes and 6 paths not run, including optional signed smoke. Final normal debug APK has QA_UNIVERSE=false and SHA256 C58C5A3869C671CBD0F245BFB0F8437C5F76FC236C915F30A59940F10F6AB397. Device checks used the same-source QA variant; the normal APK was not installed. Original 84 lots remain on the QA emulator. This accepts the bugfix delivery only: R8 sustained-rate tuning, R9 provider-wait UI, and broader Positions UX remain open. No live Russell or SP500 performance conclusion follows. The outer timeout and enrichment flow observation remain diagnostic limits, not proven resolved defects.
