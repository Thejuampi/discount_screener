---
topic: Earnings gate identities
goal: Replace YAML percents with identities on the Android pre-earnings cell
updated: 2026-09-05T21:36
---

- (event) Spec distilled from PRD Idea C lock after Advisor approve.
- (capability) CAP-1 Cheap = price < fair. Equal is Expensive. Missing fair is Undecided dcf_unavailable.
- (capability) CAP-2 High = ratio > 1. Equal is Normal. Missing or zero AR is Undecided ar_unavailable.
- (capability) CAP-3 Stale = quoteSpreadBps >= 10000 where that field is width/straddle.
- (capability) CAP-4 Hedge iff cost bps < event move bps. Prefer spread. Missing quotes Reduce.
- (capability) CAP-5 Trail cut when latest < centre - scale. Fields revenueTrailCentreCents and revenueTrailCut.
- (capability) CAP-6 Quiet >= total refuses event move. No 30 percent floor.
- (capability) CAP-7 Short put = first cheaper quoted put below ATM.
- (capability) CAP-8 YAML /2 keeps six live keys. Dropped percent keys cannot own the cell.
- (capability) CAP-9 Unsettled priced events re-run the matrix from stored pre. Settled keep the cell. Zero chain fetch.
- (capability) CAP-10 Dual-read old JSONL keys. New writes use honest names. Presentation reads them.
- (constraint) Coverage-width hedge is a non-goal. Advisor kept cost < event-move.
- (constraint) SUE off cell. robustCentre stays. Android core only.
- (event) Self-validate pass 1 coherence: each CAP has intent and success. Non-goals named. Success signal is testable.
- (event) Self-validate pass 2 preservation: Advisor F1-F8 landed. Sensei hedge P0 logged as non-goal. Examples companion holds the Cases.
- (event) sensei-advisor verdict=revise on spec. Advisor P0: CAP-5 dropped foreign-prior refuse.
- (decision) Patch Advisor S-P0-01: foreign prior refuses the trail. No cut from a contaminated window.
- (decision) Patch Advisor P1s: YAML min_revenue_trail_quarters, price_unavailable, reason order, ratio in bps, drop impliedMove fallback, CAP-8 loader reads six keys, CAP-9 append only on change.
- (constraint) Trail runs only on CheapNormal Hold. CheapHighRisk with a revenue dip still hedges.
- (event) Spec patched. Send back to Sensei+Advisor.
- (event) sensei-advisor verdict=approve on spec round 2. Sensei revise on stale-before-quiet. Advisor wins.
- (decision) Keep reason order: chain, quiet, stale, price, dcf, ar. Sensei EGI-P0-07 declined.
- (decision) Fix Advisor S2-P1-01 and S2-P1-02 in identities.md: reason Then binds the row. Trail table has foreign and prints columns.
- (event) Build: identities in engine. YAML /2. Upcoming matrix re-run. Dual-read JSONL. Docs updated.
- (event) bmad-review mixed code+docs. Lenses: adversarial (14), edge-case (4), verification-gap (5), structure (11), prose (15). Overlap: CAP-9 does not recompute event move so the 30 percent floor still owns upcoming rows; EventDecision equality appends on null vs false; encodeDefaults writes retired keys; Undecided headline color follows leftover ratio; trail card still seeds median and requires z; dual-read of sectorOverrideApplied untested; EventMove residual-below-floor test deleted; PRD still names impliedMoveBps fallback.
- (decision) Closed review holes: recompute event move on CAP-9, five-field append, omit retired JSONL keys, Undecided paints Unknown, trail line without z, restore residual-below-floor test, dual-read sector flag, YAML trail window, reason-order Cases, PRD drop impliedMove fallback.
