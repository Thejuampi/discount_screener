---
topic: Android Positions UX
updated: 2026-09-06T15:17
---

- (decision) Inherit approved PRD and PRD memlog at ../../planning-artifacts/prds/prd-android-positions-ux-2026-09-06. Parent designs and architects; Luna codes. This folder is now the spike memory home.
- (capability) CAP-1 exposure totals and weights; CAP-2 honest compact research rows and sort; CAP-3 Detail and local position facts; CAP-4 returning whole dashboard header; CAP-5 import access and small-screen accessibility.
- (decision) Core owns fixed-point portfolio math and research eligibility. Positive supported quotes contribute even when stored, with non-current disclosure. Missing quotes suppress all weights. P/L pairs eligible price and nonnegative cost; zero cost suppresses percent only. BigInteger protects intermediates; exact raw fractions set weights before two-decimal rounding. Unknown quote currency is not inferred from price; current supported book uses existing USD quote convention, no FX conversion.
- (decision) Header uses measured full group height and nested scroll from content only. Downward consumed user scroll hides after 8dp cumulative movement. Upward user movement of 8dp reveals full header. Ignore horizontal and programmatic motion. Keep list identity/state. Search focus/active state and touch exploration pin header. New composition and tab selection reveal it.
- (decision) Default sort value descending, missing last, symbol ascending ties. Needs review orders Check data, Review position, Monitor, Review opportunity, then value and symbol. Earnings sort uses existing Closeness then value and symbol. Keep existing off-feed Detail no-op; local facts expansion never dispatches or fetches. Detail uses the same book snapshot, not an independent quote.
- (constraint) Keep model and score policy unchanged. Map existing Identity primary to model comparison, Analyst range to analyst comparison, preserve Tension/Disputed/Unavailable and never infer from legacy null stance. Missing/noncurrent/untrusted/provisional evidence means Check data. Reuse known reasons with priority data issue, stale, trust, valuation missing/dispute, then earnings or signal. Update named docs and contract presentation statements. Offline tests only; no device install until live QA authorized.
- (event) Spec self-validation pass 1 coherent; pass 2 preserves approved PRD. Kernel plus architecture and examples define math, labels, UI, scope, file ownership, tests, and docs. No unresolved user question under delegated judgment.
- (event) Spec gate round 1: Sensei approve, Advisor revise S1 duplicate symbol Detail ambiguity. Resolve by rendering every exact-symbol lot in Detail, with stable input indices. Total overflow suppresses all weights. A zero-consumption content-boundary gesture restores any hidden header.
- (decision) Detail receives List<PositionsRow> filtered by exact symbol and renders every matching lot; never firstOrNull. Core total overflow suppresses weights. Tests add duplicate Detail, paired P/L numeric values, stored totals, signed half-round boundaries, and short-content-after-collapse recovery.
- (event) sensei-advisor verdict=approve on spec. Advisor resumed round 2 closes duplicate-lot ambiguity; Sensei approved with now-defined short-content recovery. User authorization covers implementation without another approval. Baseline snapshot preserved. Begin sequential Luna wave A.
- (event) PRD editorial structure and prose lenses complete. Preserve separate quote/P-L coverage requirements; accept two clarity edits. Parent drafts named operator, context, AGENTS, parity, README, index docs. Import contract has no old Opps-strip or off-feed UI statement; no contract semantics change needed.
- (event) Pre-build debug APK signature checked with apksigner. SHA-256 certificate 3695298fc8cd2070a58e9dbc0b52dd06d0272146ae86c6a9b2bef71f4d33efde. Package com.discountscreener.android. Final debug artifact must match; no device operation.
- (event) Luna wave A completes: 18 Core tests and 34 PositionsPresentation tests pass with rerun. Parent identified and corrected draft quantity expectations, missing total scale division, Int overflow, duplicate app math, empty production quote mapping, and permissive stance matching before handoff. Begin wave B header.
- (event) Parent integration audit and Luna read-only check confirm remaining wave A gaps: public-value overflow P/L pairing, stored quote override research, missing exposure coverage, duplicate Case mapping, boundary assertions. Fix after wave B and before UI. Core summary accepts total input count; both row and summary P/L require available public market value.
- (insight) Production trustSignal uses the neutral label Model value for model anchors. Treating every trustNote as a failure would make valid model rows say Check data. Exact Model value is informational; explicit Quant Lens EvidenceStrength/ExpectedValueRange Provisional or Disputed status remains a trust warning. Preserve typed source status, no score policy change.
- (event) Luna wave B completes with 17 header tests and related dashboard tests passing. Parent corrections covered natural measurement, translated placement, boundary recovery accumulation/latch, empty dispatcher, real gestures, focus height, deep-list recovery. Begin bounded domain audit fixes before UI wave.
- (decision) Header focus audit finds blank focused search lacks a Back handler. Back will clear local focus and dispatch existing ClearTickerSearch only when active. This releases the header without leaving the dashboard. Add mounted Back-release and small-motion tests before UI handoff.
- (event) Wave A audit closes with 21 Core and 42 presenter tests. Wave B focus audit closes with 19 header and 4 existing Back tests. Begin Wave C Positions and Detail UI. All tests remain offline. Parent retains architecture, documentation, integration, and final review ownership.
- (event) Read-only final data audit finds one remaining guard gap: empty or whitespace trustNote must act as absent. Schedule a bounded Core/presenter patch after Wave C to preserve sequential coding and Gradle ownership. Off-feed prices remain unavailable without existing scored evidence; no new provider path enters scope.
- (decision) Final header audit narrows boundary recovery to upward list motion. Negative unconsumed deltas occur at the bottom and must not reveal the header. Positive unconsumed deltas recover after shrink. Preserve the existing positive shrink gesture. Add mounted bottom and real programmatic component tests. This corrects browser-like intent without a new public state API.
- (event) Wave C focused suite passes 26 tests. Parent acceptance audit finds weak coverage: only Earnings sort is exercised, the normal narrow test uses 411dp, and Detail existence checks do not prove ordering or production route filtering. Tighten mounted acceptance before final review. Use projected fixtures, separate Detail Cases, common book weights, and a stateless production route composable if needed.
- (event) Final data patch passes 23 Core and 44 presenter tests with rerun. Blank notes no longer block research. Remove unused presenter quantity/availability fields and map directly from Core exposure identities. Begin bounded header bottom/programmatic patch before final UI acceptance tests.
- (event) Header audit closes: 22 focused tests pass with rerun. New bottom regression failed before the correct negative-delta guard. Mounted real LazyListState.scrollToItem coverage passes. Begin final UI acceptance patch with the approved stateless production route boundary.
- (event) Final UI acceptance passes 30 tests with rerun. Cases cover real route selection, every matching lot and whole-book weights, earnings before both lots, all sorts, and actual 320dp normal/large text without text overflow. Begin full Android gates and independent final review against the saved pre-build workspace diff.
- (event) Final review round 1 returns 10 blind findings, one duplicate edge finding, and one provider-error test gap. Parent checks every claim and records bounded patch routes in SPEC and final-review.md. The full app suite executes 1271 tests with two ImportBookScreen display failures and three skipped tests. Fix confirmed data, header, and UI issues sequentially, then repeat full gates.
- (decision) Review patch details: existing tracked quotes feed both ViewModel projection paths; unconfirmed quote age stays separate from Stored research. Eligible public values alone form partial sums. Aggregate overflow gets a reason. Saveable composition state retains sort across Detail and tabs. Remove dead import wrapper and empty totals. Bounded header viewport scrolls its own controls on short screens. Extend the existing Examples tables for these regressions.
- (event) Data review patches B1, B3, B5, B9, E1, G1 pass 26 Core and 150 presenter/ViewModel tests. Existing tracked quotes now reach both projection calls. Partial sums exclude overflowed public values. Invalid quote status and missing-analysis reasons are explicit. Begin B10 header viewport patch.
- (event) B10 passes 23 header tests with rerun. Header controls scroll within the bounded short viewport. The horizontal gesture test now targets a visible selected tab. Begin the final UI patch group B2, B4, B6, B7, B8, F1.
- (event) Independent patch readback closed B1, B3, B5, E1, G1, and B10. B9 required missing analysis before quote freshness; Luna corrected the branch and added a non-current quote test. Independent readback then closed B9. Large-text failures showed Robolectric legacy ShadowPaint character-count metrics. Parent approved native graphics for the layout tests before judging production overflow.
- (decision) Final independent patch readback closes all review findings. Full Core suite: 1619 passed and16 skipped. Full app suite:1285 passed and3 skipped. No failures or errors. All59 acceptance Cases passed as separate tests. The repository validation script passed using those outputs. Debug package and signer checks passed. Phone QA and mutation tests did not run. SPEC is done for authorized offline scope. No commit: earlier uncommitted dependencies share the touched files; preserving the user workspace takes priority over a mixed or incomplete commit. No push or install.
