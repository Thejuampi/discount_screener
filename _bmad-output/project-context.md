---
project_name: 'discount_screener'
user_name: 'Juan'
date: '2026-04-25'
sections_completed:
  - technology_stack
  - language_rules
  - framework_rules
  - testing_rules
  - quality_rules
  - workflow_rules
  - anti_patterns
  - valuation_quant_lens_rules
status: 'complete'
date_updated: '2026-08-16'
rule_count: 68
optimized_for_llm: true
handover: '_bmad-output/implementation-artifacts/handover-honest-path-street-stretch-2026-08-16.md'
---

# Project Context for AI Agents

_Critical rules and patterns AI agents must follow when implementing code in this project. Focus on details agents are likely to miss._

---

## Technology Stack & Versions

- **Monorepo:** `apps/desktop` Rust terminal workstation, `apps/windows` Tauri/React workstation, `apps/android` native Android app, `shared/contracts` cross-platform fixtures/contracts.
- **Desktop Rust:** Rust edition 2024, `crossterm 0.29.0`, `ratatui 0.30.0`, `reqwest 0.12.12` with `rustls-tls`, `rusqlite 0.32.1` with bundled SQLite, `serde 1.0.217`, `serde_json 1.0.138`.
- **Android build:** Android Gradle Plugin `8.7.3`, Kotlin `2.0.21`, Java/JVM target 17, compile/target SDK 35, min SDK 26.
- **Android runtime libraries:** Compose BOM `2024.10.01`, Activity Compose `1.9.3`, Lifecycle `2.8.7`, Coroutines `1.9.0`, Kotlin Serialization JSON `1.7.3`, OkHttp `4.12.0`.
- **Android tests:** JUnit4 `4.13.2`, JUnit Jupiter `5.11.3` in `core`, AndroidX Test Core `1.6.1`, Compose UI Test, Coroutines Test `1.9.0`, Robolectric `4.14.1`.
- **Persistence:** SQLite on desktop and Android. Desktop uses `apps/desktop/src/persistence.rs`; Android uses `SQLiteOpenHelper` in `apps/android/app/src/main/kotlin/com/discountscreener/android/data/persistence/SQLiteStateStore.kt`.
- **External data:** Yahoo Finance public endpoints/HTML parsing. Treat provider shape as live external behavior, not stable documentation.

## Critical Implementation Rules

### Language-Specific Rules

- Preserve fixed-point financial values. Fields ending in `*_cents`, `*_bps`, `*_hundredths`, and `*_millis` stay integer-based unless a strong, documented reason exists.
- Prefer type-driven invariants. Model validated symbols, bounded values, non-empty collections, state-specific variants, and mutually exclusive states in types instead of repeating runtime checks.
- In Rust, keep reusable business logic and shared data types in `apps/desktop/src/lib.rs` or the owning module. Keep `apps/desktop/src/main.rs` focused on terminal UI flow and orchestration.
- In Kotlin, keep business rules in `apps/android/core`. Android `app` code may orchestrate, persist, fetch, and present, but should not own portable domain semantics.
- Use structured parsers and serializers already present in the stack. Avoid ad hoc string parsing for JSON, SQLite payloads, or provider data when typed models are available.
- Keep comments sparse and useful. Add comments only for non-obvious behavior, invariants, or operational traps.

### Framework-Specific Rules

- Android follows strict functional-core / imperative-shell boundaries: `core/` is pure Kotlin engine/model logic; `app/` contains Android `app`, `domain`, `data`, `presentation`, and `ui` packages.
- Compose screens are passive Views. They render state and emit actions only; no network calls, SQLite calls, persistence decisions, or business-rule interpretation in Compose.
- Android Plans has three hunts. Dip (`:core` `DipSignalEngine`) gates: active-model F, signed ATR dip vs 20-day high, RSI 25–45 easing, MACD hist ≤ 0 turning or imminent, Street 12-month target ≥ 20%. Dip default universe is Opps. A session Full profile switch scans the current profile with the same gates. A cache-only 5Y MACD overlay adds +3 when 1Y and 5Y both turn, or −1 when 5Y is stuck negative. 1Y still leads. Cross (`:core` `CrossSignalEngine`) default universe is Opps. The same Full profile switch scans the current profile. Gates: active-model F, MACD hist > 0, bars since golden cross in [0, flipped_bars_max] (policy, start 3), hist slope > 0, Street 12-month target ≥ 20%, RSI ≤ 55, not knife. No ATR dip gate. 5Y overlay +3 when both years expand, −1 when 5Y is stuck negative. Leftover (`:core` `LeftoverSignalEngine`) scans the current profile: Street leftover ≤ 5% and a fading tape near the 20-day high. Leftover uses the same overlay with fade sense. F and valuation are tags on leftover. Death cross stays in. Do not change V2/V3/V4 scores. Windows Act/Wait is a separate surface. Specs: `_bmad-output/implementation-artifacts/dip-board-spec-v1.md`, `_bmad-output/implementation-artifacts/cross-board-spec-v1.md`, `_bmad-output/implementation-artifacts/leftover-board-spec-v1.md`.
- `DashboardViewModel` and other presentation classes act as Presenters. They map repository snapshots to UI state and route actions; they do not invent domain rules.
- Android `domain/` owns repository contracts and use cases. `data/` owns Yahoo client, profile loading, SQLite persistence, and repository implementation.
- `DefaultDashboardRepository` is the Android orchestration boundary for local state, Yahoo refresh, persistence, and `DashboardSnapshot` production. Keep it thin by moving pure interpretation to `core`.
- Desktop external boundaries are fixed: Yahoo fetching in `apps/desktop/src/market_data.rs`, SQLite restore/persistence in `apps/desktop/src/persistence.rs`, startup profiles in `apps/desktop/src/profiles.rs`.
- Desktop event loop state is owned by the main thread. Worker threads communicate through channels and publish events; do not add shared mutable locks around `AppState` or `TerminalState` unless architecture changes deliberately.
- Desktop rendering builds full `RenderLine` frames, then `ScreenRenderer` writes dirty rows only. Do not mix Yahoo/network/storage behavior into rendering code.

### Testing Rules

- Use strict TDD for behavior changes: write the failing test first, implement the smallest change to reach green, then refactor while tests remain green.
- Rust verification: run `cargo fmt` after Rust edits, then `cargo test` from `apps/desktop`. Use targeted `cargo test --bin discount_screener -- <name>` only for fast iteration.
- Desktop smoke verification: run `cargo run --manifest-path apps/desktop/Cargo.toml -- --smoke` for non-interactive binary validation.
- Android verification: use `scripts/validate-android.ps1` from repo root. It always runs `:core:test` and, when SDK is configured, `:app:testDebugUnitTest` and `:app:assembleDebug`.
- While a slice is still in development, iterate with `QuantHarness.hardcoded()`, then `cached()`, then `live()` only to refresh a pack (`DS_QUANT_LIVE=true`). Do not run device `make android-run-qa` until Juan says the product is ready.
- Android live QA is required when Juan says the product is ready and behavior reaches the installed app surface. Run `make android-run-qa` (boots profile **`qa`**, ≤20 symbols — never full `sp500` for agent QA; plain `make android-run` is the regular `sp500` app and is not a QA launcher). Do **not** `pm clear` or wipe app data. Verify the app launches, confirm the profile chip is QA, inspect UI/logs when relevant, and report blockers.
- **Windows live / agent / manual QA always uses universe profile `qa`.** Launch `npm run tauri:dev:qa` from `apps/windows`. Do not cold-start full `sp500` for QA unless the user explicitly orders another universe. See `Agents.md` and `docs/valuation-live-qa-checklist.md`.
- For external Yahoo/provider behavior, gather at least 5 distinct real upstream samples. Do not invent provider payloads from assumptions.
- Add persistence tests before changing SQLite schema, warm-start restore, pruning, dedupe, or migration semantics.
- Add startup/performance regression tests when changing warm restore, chart history, DCF/valuation analysis startup, or profile hydration paths.
- A pinned valuation number carries the policy version that set it. When `MODEL_POLICY_VERSION` moves, re-pin the number in the same commit and name the version. COF sat red at 16 881 cents for twelve days after `0c897087` moved residual income to 17 881, because the pin said nothing about which policy owned it.
- A cohort gate that compares model value with a Street target discounts the target one year at that row's `resolvedCostOfEquityBps` first. A target is a price twelve months out; a model value is today. See `docs/diagnostics/2026-08-29-cohort-gate-anchor/README.md`.
- Two model-quality gates are red on purpose and must not be re-pinned: `valuation_high_signal::high_signal_screener_cohort_all_members_pass` (7 of 26 members) and `operating_valuation::tests::durable_reported_and_holdout_cohorts_recompute_in_normal_gate`. They measure the model against the bar, so green comes from the model, never from the expectation.
- Android Detail: the second open of a warm ticker must paint from the ViewModel session before `ensureDetailLoaded` returns. Do not reload revision or pricing history, persist, or fetch Yahoo/SEC when memory already holds that ticker. Leftover and dip boards memo by input fingerprint. `resetInMemoryLocked` must clear `revisionHistoryHydrated`, `pricingHistoryHydrated`, `liveDcfResolvedSymbols`, replay backing, and the board memo.
- Run mutation testing around changed behavior when practical. Prefer `cargo-mutants` for Rust; otherwise perform manual mutation checks and state the gap.
- Valuation and Quant Lens changes must cover classifier routing, residual-income financials, FCFF operating path, and disagreement/Disputed EV cases — not “value near market” caps.

### Valuation & Quant Lens Rules

- **Route by business class (closed world).** `OperatingNonFinancial` → FCFF+WACC; `FinancialServices` → residual income / excess ROE on book with cost of equity; `NotEligible` → no intrinsic (ETF/fund/crypto/REIT); **`Unclassified` → valuation unavailable** (missing/unknown sector·industry — **never** silent-default to FCFF). Never run OCF−PPE CapEx FCFF as primary for banks/insurers **or managed care / healthcare plans** (ACGL/CI class of bug). **Payment networks (V, MA)** are operating FCFF even when Yahoo files them as Credit Services; **lenders (COF)** stay residual. Classifier must match `healthcare plans` / `managed care` without bare `health` (pharma stays FCFF). Policy tables must cover GICS-style operating sectors; unknown text fails closed. A parent filing that prints a material factory part and a material lender part uses `component-sotp/2`: factory FCFF is NOPAT plus depreciation minus sustaining CapEx, lender residual income uses the finance subsidiary's own annual report. Member names and the parent-plus-Financial/Credit/Capital filer name decide the split. The ticker does not. Credit-facility members are not a finance arm. Missing lender book keeps the issuer unavailable. A factory year with a non-positive margin keeps the lender value. A cyclical auto factory fades to a 300 bps through-cycle cash margin (`industry-operating-path/4`) and does not use the owner-earnings lift.

- **Parameters are dynamic.** Risk-free rate, ERP, beta (industry shrink), near-term growth (recent window), and \(g_{stable}=\min(\text{macro}, r_f-\text{buffer}, r-\varepsilon)\) come from market/policy inputs. Frozen `rf`/`ERP`/`MIN_WACC`/growth max constants are not valuation truth; defaults must be provisional when used. Android engine knobs live in `shared/contracts/valuation-policy.yaml` (`valuation-policy/1`). Kotlin objects read that file. Do not keep a second copy in source. Android production: FRED DGS10 + `ErpPolicy` implied-index school (Kroll is the overlay school; firm ICC is banned). Tests keep `MarketParams()` bootstrap. Windows still uses `from_live_risk_free` and is out of this slice.
- **Driver-based FCFF (policy/12).** Windows and Android preserve reported FCF separately but project operating FCFF from aligned annual drivers: `OCF + after-tax interest − CapEx`, recent-window revenue growth, and visible component diagnostics. The base margin is the median of the aligned annual FCFF margins (mean of the two central observations for an even count), so it never synthesizes a nonexistent year by combining independent component medians. Negative FCFF and CapEx-expansion years remain evidence instead of being silently discarded. Recent driver persistence/dispersion classifies secular expansion, stable operation, or cyclical/transition regimes without ticker exceptions. Weighted analyst mean and market price remain **external validation metrics only**—never runtime model inputs, caps, or substitutes. Default market parameters or provisional rate evidence apply `round(175 × min(debt_weight / 40%, 1))` bps; solid parameters plus solid rate evidence apply no uplift. If required drivers are insufficient or the robust aligned margin is non-positive, the engine refuses with a distinct reason. Desktop is explicitly deferred. Sustaining-CapEx treatment and the current `MODEL_POLICY_VERSION` are defined in the policy/16 rule below.
- **SEC normalization boundary.** For domestic US-GAAP `10-K`/`10-K/A` operating issuers with a CIK, normalize consolidated USD annual facts before FCFF: development CapEx, property acquisition, business acquisition, and unclassified investment are separate categories. Recurring development is the sum of plant, capitalized software, purchased intangibles (`PaymentsToAcquireIntangibleAssets`), and the well program. Drop software and intangibles when the tangible tag is `PaymentsToAcquireProductiveAssets`. The policy owns a reviewed cross-industry US-GAAP set of business and operating-asset acquisition concepts; issuer extensions remain audit candidates until explicitly reviewed. FCFF consumes recurring development only; acquisition cash remains rejected evidence. Material acquisition cash in fiscal year Y contaminates only growth from Y−1 to Y. Exclude that transition and use at least two clean recent transitions when the latest is clean; otherwise use zero near-term growth with `acquisition_normalized` provenance. Missing or misaligned approved evidence is unavailable, never zero or an imputed FCFF input. Financial-services routing still bypasses SEC FCFF. Fingerprint `sec-driver-normalization/11`. `FinanceLeaseInterestExpense` is not an interest-expense equivalent. Signed net interest uses the magnitude. A missing coupon year uses `coupon-resolution/1`: estimate from this issuer's last filed rate, or from the median of similar issuers, when confidence allows. Label the method and band. Other income and note rate ranges stay rejected. Android `debt-resolution/1` owns stock, coupon, and published k_d. Estimated coupons enter year-cash only. `issuer-market-yield/2` attaches a current USD instrument yield when Markets Insider lists the issuer. The selected k_d is the median of remaining 4–15 year yields. The yield does not shrink the tax-year window. Empty when the name or the tenor band has no quote. Do not fill k_d from a credit-index series or from a stated coupon on an OpenFIGI ticker.
- **Multi-name baseline is the policy merge bar.** Valuation engine/policy changes must keep `valuation_baseline` tests green (pinned High + ≥20% discount cohort under `apps/windows/src-tauri/tests/fixtures/valuation/`). Single-ticker greens are not enough. The 20-slot fixture requires **zero quarantines** (replace unusable names). See `_bmad-output/implementation-artifacts/valuation-multi-name-baseline-policy.md` and `Agents.md` Build section.
- **Valuation refusal must be user-visible.** `SymbolDetail.valuation_unavailable_reason` + Detail DCF slot i18n for unclassified / not-eligible / missing FCF or book — never a silent invent or mute dash without reason when backend knows why.
- **Desktop** may lag Windows WACC uplift/FCF normalize, but must **fail-closed** on unclassified (no silent FCFF). Live QA: `docs/valuation-live-qa-checklist.md` (**profile `qa` only** on Windows).
- **Tests do not replace operational discipline.** Repeat failures (one-ticker green, silent FCFF default, quarantine-as-success, full-SP500 agent thrash) are documented as mandatory procedures in root `Agents.md` → **Preventing repeat operational errors** and **Live QA = profile `qa` only** — follow those after every valuation model change.
- **Structural constraints only.** Allowed: \(g < r\), model eligibility, clean-surplus identities, missing-driver unavailability, debt-scaled provisional WACC uplift. Forbidden: hard `intrinsic/price` caps, sector FCF haircuts, silent FCFF fallback for financials, acceptance tests that only require market proximity.
- **Provenance is mandatory** for model id, business class, discount-rate kind, engine/policy version, and WACC/CoE input sources. UI labels must distinguish FCFF DCF vs residual income vs analyst.
- **Quant Lens is high-SNR.** Count independent evidence families (analyst range, model quality, history, agreement) — do not double-count gap + analyst. Strong requires solid model quality and zero conflicts. When model and analyst diverge materially, EV status is **Disputed** (show both anchors; no single absurd weighted upside). Soft model + agreement → prefer analyst as primary fair value.
- **Quant Engine is out of ranking until quality is trustworthy.** FCFF / residual income / DCF scenario width must not change `forecast_score` or `composite_score` (Windows `score_forecast_v3`, Android `OpportunityEngine` V2/V3/legacy, desktop opportunity forecasts). Detail and Quant Lens still compute and display model output. Gated by `RANKING_INCLUDES_QUANT_ENGINE = false`; re-enable only after an explicit product decision. Opening Detail must not re-order Markets via demand valuation.
- **V4 Growth is Trend + Pulse, not one Yahoo scalar.** Pulse is quarter EPS YoY (`earningsGrowth`) and is refused when `|trailing EPS| < 10¢` or when that rate is foreign to the annual net-income series (`isForeignTo`, `|z| > 3`). Trend is the median of the last two to four annual revenue YoY rates. Each takes weight 6 (half of V3's 12). Opposite signs flag `Pulse≠Trend`. Do not feed DCF `nearGrowth` or FCF growth into this rank term. V2/V3 keep the single `Growth` term.
- **V4 Forecast is one Street family.** Coverage and freshness already scale reliability; they do not add points. Target-range width takes that budget. Rating mix haircuts bipolar tails. Label the Street term as Street target, not Valuation. Do not read DCF output (`RANKING_INCLUDES_QUANT_ENGINE` stays false). V3 keeps the six-tag stack.
- **Manual analyst method (ForwardEarningsMultiple) is diagnostic-only in Slice 1C.** Project via `ValuationDossierView` / Quant Lens section `manual_analyst_method` after `primary_status` is frozen. Never feed ranking, Strong, EV blending, or FCFF router selection. Never write the candidate into `dcf_values`, `selected_valuation_values`, or `snapshots.intrinsic_value_cents`. UI must label “manual analyst method” and show metric claim, forecast period, target horizon precision, source verification, and refusal.
- **Price forecast speaks identity cash.** `price-forecast/6-identity-cash`. Street is a display family, never a forecast input. A Street-weighted mix is rejected.
- **Honesty modes.** Working identity is `ValuationHonesty.Honest`. Street-implied one-knob inversions are a parallel `NonHonest` signal (`StreetImpliedView`, policy `street-implied-honesty/3`). Compute both on one run. Detail prints both dollars in the Model block. The non-honest line names the bent input and the two rates. Publish implied bps, delta vs honest, and stretch (`Modest` / `Stretched` / `Absurd` / `Unreachable`). `ape_nh` near 0 is inversion, not a win. Do not mix Street into honest inputs or into `price-forecast`. Do not switch the working number to NonHonest. Exhaust honest path work before using non-honest freedom.
- **Detail forecast is the analyst range.** Snapshot leads with Price and Analyst. Identity Honest/Non-honest dollars sit under Model. Do not label the identity dollar as Our price. Quant Lens lists the analyst anchor first. Ranking still ignores DCF (`RANKING_INCLUDES_QUANT_ENGINE` stays false).
- **Valuation path `/33` is feature-conditional.** Hold, industry FCFF margin, quality discount, bank through-cycle ROE, and reinvestment stories come from our drivers. Street is the scoreboard only. A name whose recent median growth is at least 10% and that grew in at least three of four years is secular even when growth slowed versus the prior window. A secular name whose ROE exceeds WACC by less than 300 bps cannot floor near-term growth at the mature cap. Internet retail/content may expand margin to the industry target only when excess ROE is positive. A bank below through-cycle ROE may lift at most 400 bps in year 1; it cannot jump the full 13% floor from a 6% ROE. Managed-care persist of 2000 bps is for ROE of 20% or more; mid-teens ROE uses the modest 350 bps spread.
- **Valuation judgment is a core object above identity.** `DcfAnalysisEngine.compute()` stays exclusive (one class, one model, or refuse). `ValuationJudgment` reads identity, optional justified multiple, and Street. Stance is `Street` / `Identity` / `Unavailable` in production (`valuation-judgment/3`, street-first product call 2026-08-21); `Tension` and `Disputed` remain enum values for parity and replay and never name a primary. The analyst range decides: a usable Street book is the primary whatever our model says; our model speaks alone only when no usable Street exists, and the card says so. Class refuse (`Unclassified` / `NotEligible`) does not hide a complete Street: its refusal code rides onto the judgment beside `StreetPrimary`, as do `SoftIdentity` and `IncomparableAnchors` where they apply. Relation reports `SingleSource` under street-first even with two complete series. Identity is incomplete when `scenarioWidthBps` is null or above `IDENTITY_USABLE_MAX_WIDTH_BPS` (12000). Soft is not complete above that cut. There is no third unusable-complete state. FEM is attached, not primary, until a later spec. Android Detail and list rows read a projection snapshot. List Disc% and Upside% name `primaryCents` only. With no primary the row shows the stance token, never a Street-first gap. Official stance gap is `ValuationDecisionPolicy.differenceBps`, never a one-sided percent versus price. Reference lines render only where the primary does not already show those series (no-primary branch of the judgment card). Spec: `_bmad-output/implementation-artifacts/spec-valuation-judgment-core-2026-08-15.md` (Status rules section carries the 2026-08-21 supersession).
- **Valuation decision policy is core-owned.** Keep `Availability`, `Coverage`, freshness, confidence, and relation separate. For compatible positive anchors use integer half-up `differenceBps`; reduce all pairs to `Aligned` (≤2500), `Tension` (2501–5000), or `Disputed` (>5000). Never synthesize cross-provider analyst consensus. TipRanks is per-symbol, cache-first, explicit-load-only, and needs three distinct observations no older than 90 days before it is decision eligible.
- **TipRanks credentials and budget are durable boundaries.** Keep API keys in an Android Keystore AES-GCM envelope excluded from backup; never log/render them. Persist forecast cache separately from a conservative `reserved`/`sent` ledger and provider usage snapshots. No automatic retries; only explicit forecast calls consume the monthly forecast budget.
- **Contracts:** `shared/contracts/valuation-model-family.json` and source-selection contracts; keep Windows/Android/desktop engines aligned.
- Design authority: `_bmad-output/planning-artifacts/valuation-model-family-architecture.md` and root `AGENTS.md` / `Agents.md`.
- **High-signal screener cohort (goal gate).** Contract `valuation-high-signal-screener-cohort-v1.json`; Windows `cargo test --lib valuation_high_signal::` **recomputes** Yahoo+SEC+live 10Y. Success = all pinned names solid/scale-coherent/class-correct with Street band as diagnostic only. Soft provisional bootstrap is not success. No clamp-to-Street, no observation hand-edit to green. Last handoff progress ~16/26 — not done.
- **Gap attribution waterfall (telemetry).** Module `valuation_gap_attribution.rs`; contract `valuation-gap-attribution-v1.json`. Shapley over `{rates,horizon,path,g_terminal}`; factor baselines are **own policy** (unit-β, hold0/fade10, growth0) — never Street reverse-engineering. Street only on `diagnostic_gap_vs_street_*`; forbidden as acceptance/optimand (enforcement scan). Horizon attr=$0 when active hold/fade equal baseline is correct, not a Shapley bug.
- **Naive FCFF diagnostic uses WACC + net debt.** Firm FCFF → discount at **WACC** → EV − net_debt → equity/share. Never CoE-on-unlevered or EV/share without debt bridge. Extract run-rate via `extract_normalized_fcff_level` (no ordered scenarios required).
- **Sustaining CapEx is earned by growth, never by profitability (policy/16).** `maintenance = normalized_capex_intensity × δ/(δ+g)` from the steady-state capital identity `κ = c·(δ+g)`, with `δ = ASSET_RENEWAL_RATE_BPS = 1000` (~10y asset life) and `g` = near-term revenue growth floored at 0; result clamped to `≤ κ`. Policy/15 used `min(CapEx p25, 15% of OCF margin)`, which made maintenance a function of margin — a 28%-OCF cable network was charged ~4% of revenue against ~20% actual reinvestment (CHTR FCFF/sh ~$141 vs external ~$30–70). Under /16 flat-growth networks stop qualifying as investment waves and keep the reported FCFF identity; genuine compounders keep owner earnings. Do not reintroduce a profitability-linked cap. `MODEL_POLICY_VERSION = business-class-policy/30-median-retention`. Residual income fades extra profit toward min(ROE0, cost of equity + 500 bps), then capitalizes leftover extra profit at policy stable growth. Raw ROE0 does not run forever. FCFF near-term growth stays within 1200 bps of g_stable. OCF level uses the latest year when the recent path is non-decreasing **and** the prior window already printed at least two positive OCF years (a franchise to restore). A first-cash ramp (UBER-class) keeps the recent OCF centre. A CapEx spike does not drop that year's OCF. Terminal growth does not exceed demonstrated recent growth. A grower whose CapEx exceeds sustaining CapEx uses owner earnings; a flat high-CapEx network keeps reported FCFF. Cost of debt uses market yield, then a rated or coverage-synthetic spread, then the accounting coupon. Coverage is EBIT / interest with EBIT = pretax + interest. The cheap coupon on old debt does not set WACC. A name whose recent median growth is at least 10% and that grew in at least three of four years is secular. One soft year does not make it a cycle. A year that reports OCF, CapEx, and revenue but not a separate interest line still sets starting revenue and growth. It does not invent a coupon. The FCFF identity stays on years that disclose interest and tax. Per-share equity uses shares outstanding today when that count sits between 90% and 115% of the year's diluted average. A buyback year stays in that band. A dual-class quote (GOOGL Class A vs A+C) does not. That gap keeps the diluted average. Historical buybacks already sit in the current count. Future buybacks at a price above model value are not added as accretion. Residual-income retention uses the median of the same recent years as ROE. A trough earnings year with sticky dividends does not set perpetual payout. Policy/16 FCFF sustaining-CapEx rule is unchanged. FCFF bear/bull growth is a 400 bps band around the median, not the raw year-to-year IQR stacked over the projection. Driver OCF/CapEx/growth use the last 4 aligned years so a 6-year AMZN pack does not keep the 2021 trough after a 2025 CapEx spike.
- **Overvalued mechanisms differ by name.** CHTR: most of Street gap already in EPS@unit-β baseline. T: most of gap from active rates+path policy. Never generalize one name to the whole overvalued cluster.
- **Numerical conclusions protocol.** Before presenting $ figures as conclusions: sniff vs 10-K/fixture OOM (~50%); ≥2 names for cluster claims; declare baseline before counterfactuals; mark dubious inputs pending. Full text in `Agents.md`.
- **Current continuity (identity / Street stretch):** `_bmad-output/implementation-artifacts/handover-honest-path-street-stretch-2026-08-16.md`. Older Windows motor brief stays at `handover-quant-valuation-engine-2026-08-02.md`.

### Analyst-Method Evidence Lifecycle Rules

- **Evidence binding is semantic.** Frozen IDs and numeric units are necessary but insufficient: EPS and multiple observations must prove compatible metric roles, basis, fiscal period/horizon, currency/share basis, and lineage.
- **Trusted revision is publish-or-invalidate.** After a control envelope safely establishes issuer/security/projection/supersession authority, semantic refusal must atomically invalidate the stale current candidate; malformed or foreign envelopes must not invalidate anything.
- **Idempotency is command identity.** Persist and fingerprint explicit role bindings plus evidence set, immutable identity/share vintage, method/engine/policy, replay mode, the stable semantic `decisionAt`, projection key, and supersession intent. Processing/retry time is separate: identical transport retried later is a no-op, while a decision-only mutation conflicts. Equal results do not make two commands identical.
- **Current is eligibility, not a pointer.** A candidate is current only while operational, non-invalidated, policy/engine-current, identity/share-vintage-current, and fully reconstructible from frozen command, membership, roles, result, supersession and revision lineage. Raw current-pointer access is not a publication API.
- **Lineage integrity is transitive.** Validate the entire reachable revision ancestry, not only the candidate edge: every predecessor exists, has one consistent intent, remains in issuer/security partition, and no node is revisited.
- **Closure is independently earned.** Domain-hard slices move from design-ready → implemented → independently closed. Before the next consuming slice, run an adversarial invariant matrix, per-field negative mutations, transaction rollback/reopen, and reconcile durable status artifacts with code and gates.
- **Publication read is eligibility-gated.** `load_analyst_method_publication` / `eligible_current_projection_run_id` reconstruct the full command, membership, roles, identity, result, supersession and revision lineage before exposing a current candidate; raw projection pointers are not a UI API.

### Code Quality & Style Rules

- Prefer extending the module that owns the concern over adding cross-cutting logic to entrypoints or UI files.
- Keep Android UI dense and operational. This app is a workstation-style financial tool, not a marketing surface.
- Keep UI text professional and data-focused. Do not add visible instructional prose that describes features, shortcuts, or implementation details.
- Avoid nested cards and decorative UI. Use cards only for repeated items, modals, and genuinely framed tools.
- Keep chart and toolbar dimensions stable. Dynamic labels, hover states, replay controls, and chips must not shift layout unpredictably.
- Use existing helpers and fixtures in tests before hand-building large snapshots inline.
- Keep temp operations inside `.agents/workspace/tmp`.
- Do not duplicate long operational guidance in code comments. Update or link existing docs when user-visible behavior changes.

### Development Workflow Rules

- **Single-user, single-developer delivery.** Juan is the only product user and Codex is the implementation partner. Use BMAD documents when they preserve product, architecture, contract, or verification decisions, but do not introduce sprints, velocity, backlog grooming, story points, role-play handoffs, or Agile ceremony unless Juan explicitly requests them. For substantial changes, record the decision/spec and then implement directly with TDD and the required gates.
- Respect dirty worktrees. Never revert user changes or unrelated files. If existing changes affect the task, work with them and call out conflicts only when necessary.
- Use `apply_patch` for manual edits. Avoid shell write tricks for source changes.
- Use `rg`/`rg --files` first for search. Prefer parallel reads for independent file inspection.
- For Android SDK-dependent work, ensure `ANDROID_HOME`, `ANDROID_SDK_ROOT`, or `apps/android/local.properties` is configured before expecting app Gradle tasks to pass.
- For live Android QA, use `make android-run-qa` from repo root. It builds, installs with the QA flag, boots **`qa`** membership, and launches. It does not clear app data. Run artifacts go to `.agents/workspace/tmp/android-run`. Do not QA on full SP500.
- For cross-platform behavior changes, update or add shared contract fixtures under `shared/contracts` and verify both platform interpretations where applicable.
- When a feature depends on local persistence plus app UI, validate three layers: pure rule/unit tests, SQLite/repository tests, and installed-app behavior.

### Critical Don't-Miss Rules

- **Startup must stay bounded.** Warm start restores only the current/bounded state needed for initial UI readiness. Do not load or backfill complete per-symbol pricing/history data during app startup.
- **Complete pricing/history data is per-ticker and on-demand.** Load full saved candle history when opening/selecting a ticker detail or history surface, not globally.
- **SQLite is persistence infrastructure, not a domain-rules dumping ground.** Put dedupe/merge semantics in pure core logic where possible; enforce uniqueness in SQLite as a boundary guarantee.
- **Historical revisions are facts.** Do not rebuild past revisions from current in-memory detail; persisted history must reconstruct the original saved payload truthfully.
- **Yahoo data is unstable.** Parser or provider-integration changes need live samples and failure behavior for missing/sparse fields.
- **No business rules in Compose.** If a rule affects valuation, ranking, provenance, dedupe, confidence, or trend interpretation, move it upstream.
- **No network/storage in rendering.** Rendering should consume state already prepared by the app or core layers.
- **Preserve decoupling between market data, persistence, and UI.** Mixing them caused regressions and makes live QA harder.
- **Do not hide sparse data states.** Show empty, stale, unavailable, no-baseline, and no-analyst-target states explicitly rather than smoothing or omitting them.
- **Prefer demand-driven expensive work.** History loading, valuation/DCF analysis, Yahoo fetches, and Android startup flows must be bounded, cancellable where practical, and observable enough to debug. Financial residual income may run on fundamentals ingest or Quant Lens demand; operating FCFF still needs cash-flow history.
- **Live QA findings override build confidence.** Passing unit tests and Gradle builds is insufficient when the installed app hangs, fails to launch, or renders the wrong surface.
- **Do not “fix” valuation noise with output clamps.** Fix model routing, driver definitions, parameter dynamics, and Quant Lens agreement policy instead.

---

## Usage Guidelines

**For AI Agents:**

- Read this file and root `Agents.md` / `AGENTS.md` before implementing code in this repository.
- Follow all rules exactly as documented.
- When rules conflict, prefer the stricter boundary or verification requirement.
- Update this file when new durable project patterns emerge (especially valuation and scoring).

**For Humans:**

- Keep this file lean and focused on agent needs.
- Update it when technology versions, architecture boundaries, or verification gates change.
- Remove rules that become obsolete or too obvious to preserve LLM context efficiency.

Last Updated: 2026-08-16
