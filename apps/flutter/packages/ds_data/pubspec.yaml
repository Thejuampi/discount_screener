name: ds_data
description: Network, SQLite, and profile data layer for Discount Screener Flutter.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  ds_core:
    path: ../ds_core
  http: ^1.4.0
  meta: ^1.16.0
  path: ^1.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
