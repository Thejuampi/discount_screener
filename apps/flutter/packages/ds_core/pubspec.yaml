name: ds_core
description: Pure Dart domain models and engines for Discount Screener (port of apps/android/core).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  collection: ^1.19.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
