package com.discountscreener.android.ui.dashboard

/** Names the field, and says whose sentence it is. */
internal const val SYMBOL_NOTE_LABEL = "My note"

/**
 * Says what the note does to the score, which is nothing.
 *
 * A sentence printed under four numbers reads as one of their inputs unless it is told otherwise.
 * The note exists for what the app cannot read from a filing — a partnership that ends, a lawsuit,
 * a plant that burned — and none of that reaches the engine.
 */
internal const val SYMBOL_NOTE_HINT = "Yours to write. Nothing here changes the score."

/** The button appears only when there is an unsaved change, so its label can say what it does. */
internal const val SYMBOL_NOTE_SAVE_LABEL = "Save note"
