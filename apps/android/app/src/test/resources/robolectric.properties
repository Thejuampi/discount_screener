# See RobolectricTestApplication: it registers androidx.activity.ComponentActivity so the Compose
# rules can launch it on the release variant too, where the ui-test-manifest entry is absent by
# design. Set here rather than per class so no test can forget it.
application=com.discountscreener.android.RobolectricTestApplication
